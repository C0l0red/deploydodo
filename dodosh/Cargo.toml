[package]
name = "dodosh"
version = "0.1.0"
edition = "2021"

[dependencies]
russh = "0.60"
pty-process = { version = "0.4", features = ["async"] }
thiserror = { workspace = true }
tokio = { workspace = true }
